function processSchemas(schemas) {
  // Process schemas, handling nested structures
  // Extract WebSite, WebPage, WPHeader, SiteNavigationElement data
  // Return processed data
}

function generateHTML(processedData) {
  // Generate HTML based on processed schema data
  // Create H1 from WebSite name
  // Create menu from SiteNavigationElement names
  // Return generated HTML
}

function createSplitView(originalContent, schemaHTML) {
  // Create two side-by-side divs/iframes
  // Populate one with originalContent, the other with schemaHTML
}